<% if (typeof recentRelease !== 'undefined' && recentRelease) { %>
  <a class="update-banner" href="/changelog">🎉 There's a new update (v<%= recentRelease.version %>) — click here to see what's new</a>
<% } %>
